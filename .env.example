# MiniMax API Configuration (REQUIRED)
# Get your JWT token from https://www.minimaxi.com/
# IMPORTANT: Token must be on ONE SINGLE LINE with no line breaks or spaces
# Format: header.payload.signature (three base64url segments separated by dots)
# Example format: eyJhbGci...eyJzdWI...SflKxw (do NOT use this example - get your real token)
MINIMAX_JWT_TOKEN=

# Runtime Behavior Configuration (OPTIONAL)

# Environment mode: development, production, or test
NODE_ENV=development

# Logging level: debug, info, warn, error
LOG_LEVEL=info

# Maximum message length for thinking block truncation
# Controls how much of the thinking text is displayed in console output
# Default: 300 characters
# Valid range: 50-1000 characters
# Uses smart truncation algorithm: keeps first 2/3 and last 1/3 of text
# Example: "This is a long thinking block..." → "This is a long th...king block message"
MAX_MESSAGE_LENGTH=

# Console screen clearing on CLI startup
# Controls whether the interactive CLI clears the terminal screen on startup
# Default: true (screen is cleared for clean interactive experience)
# Accepted values: true/false, 1/0, yes/no (case-insensitive)
# CLI flag override: --no-clear flag takes precedence over this setting
# Note: Only affects interactive TTY sessions; non-interactive mode never clears
CLEAR_SCREEN=
# Example: CLEAR_SCREEN=false to preserve previous terminal output
# Example: CLEAR_SCREEN=no to keep terminal history visible on startup
# Example: CLEAR_SCREEN=0 to disable clearing

# Optional: Claude CLI path (if you have it installed)
# CLI_PATH=/path/to/claude-cli
